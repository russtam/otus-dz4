apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rustam-chart.fullname" . }}-test-connection"
  labels:
    {{- include "rustam-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rustam-chart.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
